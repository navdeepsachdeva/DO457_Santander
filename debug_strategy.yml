- name: Debug Strategy Demo
  hosts: iosxe1*
  gather_facts: false
  strategy: debug
  tasks:
    - name: Task 1
      ansible.builtin.command: sleep 2
      register: pawel_out

    - name: Task 20
      ansible.builtin.command: sleep 2

    - name: Configure the login banner 21
      cisco.ios.ios_banner:
        banner: motddd
        text: Hello Jodi {{ pawel_out.changed }}
        stateeee: present
      # task.args
      # task.args['banner']='motd'
      # task.args['state']='present'
      # del(task.args['stateeee'])
      # task.args
      # redo

    - name: Save Configuration 22
      cisco.ios.ios_config:
        save_when: alwaysss
      # task.args
      # task.args['save_when']='always'
      # task.args
      # redo

    - name: Task 23
      ansible.builtin.command: sleep 2
